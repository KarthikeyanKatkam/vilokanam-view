[package]
name = "ocw-ticker"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
anyhow = "1"
hex = "0.4"
parity-scale-codec = { version = "3.6", features = ["derive"] }
# ❌ remove sp-core / sp-runtime  -> use only SCALE + hex